syntax = "proto3";

option go_package = "pkg/pb/";

service RoomService {
    rpc Create(CreateRoomRequest) returns (Room) {}
}

message CreateRoomRequest {
    string name = 1;
    string password = 2;
}

message Room {
    string id = 1;
    string name = 2;
}
